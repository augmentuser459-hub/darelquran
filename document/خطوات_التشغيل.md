# 🚀 خطوات تشغيل نظام دار القرآن - دليل كامل

## ✅ تم حل مشكلة الاتصال بقاعدة البيانات!

**التاريخ:** 7 يناير 2026  
**الحالة:** ✅ يعمل بنجاح

---

## 📋 المشكلة التي تم حلها

كانت المشكلة في عدم تطابق إعدادات الاتصال:
- ملف `.env` كان يستخدم Direct Connection (لا يعمل)
- ملف `run.bat` كان يستخدم Pooler Connection (يعمل)

**الحل:** تم توحيد الإعدادات لاستخدام Pooler Connection الذي يعمل بنجاح.

---

## 🔧 الإعدادات الصحيحة (تم تطبيقها)

### ملف `.env` (محدث):
```env
DB_NAME=postgres
DB_USER=postgres.xydqfdqvbjmjrebysfzz
DB_PASSWORD=dar-quran12345
DB_HOST=aws-1-eu-west-1.pooler.supabase.com
DB_PORT=6543
```

### معلومات قاعدة البيانات:
- ✅ PostgreSQL 17.6
- ✅ 38 جدول
- ✅ 10 دول
- ✅ 4 أنظمة تسعير
- ✅ 1 محفظ
- ✅ 1 طالب

---

## 🚀 خطوات التشغيل

### الطريقة 1: استخدام run.bat (الأسهل)

```bash
# في مجلد المشروع
run.bat
```

### الطريقة 2: يدوياً

```bash
# 1. تفعيل Virtual Environment
venv\Scripts\activate

# 2. تشغيل السيرفر
python manage.py runserver
```

---

## 🌐 الوصول للنظام

### Backend (Django):
- **URL:** http://127.0.0.1:8000/
- **Admin Panel:** http://127.0.0.1:8000/admin/
  - Username: `admin`
  - Password: `admin123456`

### API Endpoints:
- **API Root:** http://127.0.0.1:8000/api/
- **الطلبة:** http://127.0.0.1:8000/api/students/
- **المحفظين:** http://127.0.0.1:8000/api/teachers/
- **الحصص:** http://127.0.0.1:8000/api/sessions/
- **الفواتير:** http://127.0.0.1:8000/api/invoices/

### Frontend:
```
افتح في المتصفح:
frontend/index.html
```

---

## ✅ التحقق من أن كل شيء يعمل

### 1. اختبار الاتصال بقاعدة البيانات:
```bash
python test_django_connection.py
```

**النتيجة المتوقعة:**
```
✅ الاتصال ناجح!
PostgreSQL: PostgreSQL 17.6
📊 البيانات الموجودة:
   - الدول: 10
   - أنظمة التسعير: 4
   - المحفظين: 1
   - الطلبة: 1
```

### 2. اختبار Django:
```bash
python manage.py check
```

**النتيجة المتوقعة:**
```
System check identified no issues (0 silenced).
```

### 3. اختبار API:
افتح في المتصفح:
```
http://127.0.0.1:8000/api/
```

يجب أن ترى قائمة بجميع API Endpoints.

---

## 📱 الصفحات المتاحة

### ✅ مكتملة وجاهزة:

1. **لوحة التحكم** (`frontend/index.html`)
   - إحصائيات سريعة
   - رسوم بيانية (الحضور والإيرادات)
   - حصص اليوم
   - آخر المدفوعات

2. **إدارة الطلبة** (`frontend/pages/students.html`)
   - قائمة الطلبة مع بحث وفلترة
   - إضافة/تعديل/حذف طالب
   - إحصائيات الطلبة

3. **إدارة المحفظين** (`frontend/pages/teachers.html`)
   - قائمة المحفظين مع بحث وفلترة
   - إضافة/تعديل/حذف محفظ
   - إحصائيات المحفظين

4. **Admin Panel** (`http://127.0.0.1:8000/admin/`)
   - إدارة كاملة لجميع البيانات
   - 18 Model متاح
   - واجهة Django الإدارية

---

## 🔍 حل المشاكل

### مشكلة: السيرفر لا يعمل

**الحل:**
```bash
# 1. تأكد من تفعيل Virtual Environment
venv\Scripts\activate

# 2. تأكد من الاتصال بقاعدة البيانات
python test_django_connection.py

# 3. تحقق من عدم وجود أخطاء
python manage.py check

# 4. أعد تشغيل السيرفر
python manage.py runserver
```

### مشكلة: خطأ في الاتصال بقاعدة البيانات

**الحل:**
```bash
# 1. تحقق من ملف .env
# يجب أن يحتوي على:
DB_HOST=aws-1-eu-west-1.pooler.supabase.com
DB_PORT=6543
DB_USER=postgres.xydqfdqvbjmjrebysfzz

# 2. اختبر الاتصال
python test_connection_fix.py

# 3. إذا فشل، تحقق من أن مشروع Supabase نشط
```

### مشكلة: Frontend لا يتصل بـ Backend

**الحل:**
```javascript
// 1. تأكد من أن Backend يعمل
// افتح: http://127.0.0.1:8000/api/

// 2. تحقق من frontend/js/config.js
API_BASE_URL: 'http://localhost:8000/api'

// 3. افتح Console في المتصفح (F12)
// ابحث عن أخطاء CORS أو Network
```

---

## 📊 هيكل المشروع

```
dar-quran/
├── core/                      # Django App
│   ├── models.py             # 18 Models
│   ├── serializers.py        # 18 Serializers
│   ├── views.py              # 18 ViewSets
│   ├── urls.py               # API URLs
│   └── admin.py              # Admin Panel
├── frontend/                  # الواجهة الأمامية
│   ├── index.html            # لوحة التحكم
│   ├── pages/
│   │   ├── students.html     # إدارة الطلبة
│   │   └── teachers.html     # إدارة المحفظين
│   ├── css/style.css         # التصميم
│   └── js/                   # JavaScript
├── .env                       # إعدادات قاعدة البيانات (محدث)
├── run.bat                    # ملف التشغيل (محدث)
├── manage.py                  # Django Management
└── requirements.txt           # المكتبات المطلوبة
```

---

## 🎯 الخطوات التالية

### للمطورين:
1. ✅ Backend جاهز 100%
2. ✅ قاعدة البيانات متصلة
3. 🔄 إكمال باقي صفحات Frontend (75% متبقي)
4. 🔄 إضافة Authentication
5. 🔄 إضافة Permissions

### للمستخدمين:
1. ✅ تشغيل النظام
2. ✅ الدخول للـ Admin Panel
3. ✅ استكشاف الصفحات المتاحة
4. 🔄 إضافة بيانات تجريبية
5. 🔄 اختبار الوظائف

---

## 📝 ملاحظات مهمة

### قاعدة البيانات:
- ✅ Supabase PostgreSQL 17.6
- ✅ Pooler Connection (Port 6543)
- ✅ SSL مفعل
- ✅ 38 جدول جاهز
- ✅ بيانات أولية موجودة

### الأمان:
- ⚠️ DEBUG=True (للتطوير فقط)
- ⚠️ SECRET_KEY يجب تغييره في الإنتاج
- ⚠️ AllowAny permissions (للتطوير فقط)

### الأداء:
- ✅ Connection Pooling مفعل
- ✅ Persistent Connections
- ✅ Connection Health Checks
- ✅ Statement Timeout (30 ثانية)

---

## 🎉 الخلاصة

تم بنجاح:
- ✅ حل مشكلة الاتصال بقاعدة البيانات
- ✅ توحيد إعدادات الاتصال في `.env` و `run.bat`
- ✅ اختبار الاتصال والتأكد من عمله
- ✅ تشغيل السيرفر بنجاح
- ✅ جميع API Endpoints تعمل
- ✅ Admin Panel جاهز
- ✅ Frontend جاهز (3 صفحات)

**النظام جاهز للاستخدام! 🚀**

---

## 📞 الدعم

إذا واجهت أي مشكلة:
1. راجع هذا الملف
2. اختبر الاتصال: `python test_django_connection.py`
3. تحقق من الأخطاء: `python manage.py check`
4. راجع ملفات التوثيق في مجلد `document/`

---

**آخر تحديث:** 7 يناير 2026 - 20:26  
**الحالة:** ✅ يعمل بنجاح  
**السيرفر:** http://127.0.0.1:8000/
